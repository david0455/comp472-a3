# Model 2: NB-BOW-FV
# filter out the words that appear only once in the
# training set, so V contains only the words that appear at least 2 times in the training set.

def bow_fv():
    print("hello")
